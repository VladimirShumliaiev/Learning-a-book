import "./App.css";

function App() {
  const frederick = {
    name: "Frederick",
    strong: true,
    fast: true,
  };

  const reFrederick = (person) => ({});

  return <>O.REilLY-REACT</>;
}

export default App;
